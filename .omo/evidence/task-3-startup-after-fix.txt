=== VERCEL DEV STARTUP OUTPUT (AFTER FIX) ===

Config: buildCommand="", outputDirectory="public", functions={api/[...path].js: maxDuration=60}, rewrites

Key observations:
- Imported Builder "@vercel/node" — NODE BUILDER IS LOADED
- Resolved builders: "@vercel/node@latest => 5.6.9, @vercel/static => undefined"
  -- NOTE: @vercel/static is used instead of @vercel/static-build (no Vite/yarn issue)
- Adding build match for "api/[...path]" with "@vercel/node@latest"
- Adding build match for static assets (index.html, favicon, etc.)
- Ready! Available at http://localhost:3000
- GET /api/view?q=ORC-20261143 — API function invoked (returns 404 "Orçamento não encontrado")
- Proxying to "@vercel/node" dev server (port=50372, pid=29776)
- GET / — Frontend served (200 OK with HTML)
- Static assets served from public/

=== CURL TEST RESULTS ===
1. API: curl http://localhost:3000/api/view?q=ORC-20261143 → 404 "Orçamento não encontrado" (WORKING)
2. Frontend: curl http://localhost:3000/ → 200 HTML (WORKING)

=== FIX DETAILS ===
The fix uses buildCommand="" (empty string) + outputDirectory="public".
This prevents @vercel/static-build from triggering (which blocks @vercel/node),
and instead lets @vercel/static serve the pre-built files from public/.

Note: The originally prescribed fix (devCommand: "vite", no buildCommand, no outputDirectory)
was tested and DOES NOT WORK — with devCommand, @vercel/node is never imported/compiled,
causing all API requests to timeout.

=== VERCEL CLI VERSION === 50.25.4
