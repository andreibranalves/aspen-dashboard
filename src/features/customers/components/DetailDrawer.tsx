/**
 * DetailDrawer — alias de compatibilidade para o Drawer canônico do
 * design system. Importe de `@/components/shared/Drawer` em telas novas.
 */
export { Drawer as DetailDrawer } from '@/components/shared/Drawer';
export type { DrawerProps as DetailDrawerProps } from '@/components/shared/Drawer';
