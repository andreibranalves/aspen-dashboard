// migrate-tokens.mjs — bulk replace framer/shadcn token classes → Alpine vocabulary
// (stub — implementar quando for necessário migrar tokens novamente)

console.log('[migrate-tokens] nenhuma migração pendente');
