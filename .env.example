APP_ENV=development
EXTERNAL_WRITES_ENABLED=0

OPENROUTER_API_KEY=sk-or-...aqui
OPENROUTER_MODEL=google/gemini-2.5-flash
DATABASE_URL=
TEST_DATABASE_URL=
RESTORE_DATABASE_URL=
RESTORE_PG_SERVICE=

# Existing backup/cutover flow. Not used by the HIGH migration preflight.
CUTOVER_PG_SERVICE=

# Protected libpq files supplied by the approved operator environment.
PGSERVICEFILE=
PGPASSFILE=

# Staging migration target. Never inherit DATABASE_URL for a HIGH migration.
STAGING_DATABASE_URL=
STAGING_PG_SERVICE=
PRODUCTION_DATABASE_URL=

# PostgreSQL-only cutover names-only manifest. Keep values outside this checkout.
STAGING_BASE_URL=
E2E_USERNAME=
E2E_PASSWORD=
STAGING_E2E_USERNAME=
KNOWN_POSTGRES_QUOTATION_ID=
KNOWN_POSTGRES_SCRATCH_QUOTATION_ID=
STAGING_EGRESS_BLOCKED=
STAGING_FIXTURE_RESET=
CANARY_BASE_URL=
CANARY_PASSWORD=
CANARY_QUOTATION_ID=
CANARY_PUBLIC_QUOTATION_URL=
PRODUCTION_PG_SERVICE=
PRODUCTION_CANARY_PASSWORD=
KNOWN_PRODUCTION_POSTGRES_QUOTATION_ID=
KNOWN_PRODUCTION_PUBLIC_QUOTATION_URL=
PREVIEW_DEPLOYMENT_URL=
PREVIOUS_PRODUCTION_DEPLOYMENT_URL=
POST_CLEANUP_PREVIEW_URL=

APP_PASSWORD_HASH=
APP_SESSION_SECRET=
APP_AUTH_BYPASS=false
SMTP_PASSWORD=
EVOLUTION_BASE_URL=https://evolution-api-qeaa.srv1633500.hstgr.cloud
EVOLUTION_API_KEY=
EVOLUTION_INSTANCE=
# Vercel Blob — auto-injected when you connect a Blob Store to the Vercel project.
# For local dev, create a Blob Store in Vercel Dashboard → Storage → Blob → Create.
BLOB_READ_WRITE_TOKEN=
# Dedicated private store for definitive quotation PDFs. Use either its token
# or its store ID with Vercel OIDC; do not point this at the public media store.
QUOTATION_BLOB_READ_WRITE_TOKEN=
QUOTATION_BLOB_STORE_ID=

KV_REST_API_URL=
KV_REST_API_TOKEN=
